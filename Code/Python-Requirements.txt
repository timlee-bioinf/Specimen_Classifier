# Tested using the following (Anaconda's base environment):

Python==3.12.8
tensorflow==2.21.0
numpy
pandas
matplotlib
scikit-learn
Pillow

# numpy, pandas, matplotlib, scikit-learn, and Pillow all ship with the default
# Anaconda installer. The only package you need to install yourself is tensorflow:
#   pip install tensorflow